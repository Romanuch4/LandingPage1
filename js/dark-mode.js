(function () {
  const options = {
    cursor: 'pointer',
    time: '0.5s', // default: '0.3s'
    label: '🌓', // default: ''
  };

  const darkmode = new Darkmode(options);
  darkmode.showWidget();
})();
